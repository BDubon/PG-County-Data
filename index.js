var express = require('express');
var app = express();

var server = app.listen(1337, fucntion() {
    var host = server.address().address;
    var port = server.address().port;

    console.log('Your app is running at http://%s:s%', host, port)
});

